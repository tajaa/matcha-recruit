// ChannelView — slim entry point. The implementation was split into the
// sibling `ChannelView/` folder (logic hook + presentational components) on
// 2026-07-18; the public import path (`work/pages/ChannelView`) and default
// export are unchanged.
export { default } from './ChannelView/ChannelViewScreen'
