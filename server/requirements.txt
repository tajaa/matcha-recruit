fastapi>=0.104.0
uvicorn[standard]>=0.24.0
asyncpg>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.4.0
psycopg[binary]>=3.1.0
google-genai>=2.0.0,<3.0.0
google-auth>=2.40.0,<3.0.0
pymupdf>=1.23.0
pypdf>=4.0.0
python-docx>=1.1.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
email-validator>=2.0.0
httpx>=0.25.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
weasyprint>=60.0
boto3>=1.34.0
celery>=5.3.0
redis>=5.0.0
alembic>=1.13.0
sqlalchemy[asyncio]>=2.0.25
greenlet>=3.0.0
feedparser>=6.0.0
stripe>=8.0.0
python3-saml>=1.16.0
# Pinned exactly, not floated: the ~400MB Chromium bundle is baked into the
# Dockerfile `base` stage so a dep bump doesn't force a re-push of it. That
# stage installs browsers for PLAYWRIGHT_VERSION, which MUST equal this pin —
# the runtime build asserts it and fails loudly on skew.
playwright==1.62.0
twilio>=9.0.0
audioop-lts>=0.2.1; python_version >= "3.13"
aioapns>=3.1
bleach>=6.1.0
cachetools>=5.0
livekit-api>=0.6
markdown>=3.5
segno>=1.6.0
openpyxl>=3.1
# Already present transitively (weasyprint); pinned explicitly because Merlin's
# chat-image path imports it directly to downscale attachments before they go
# into a prompt. Code degrades to sending the original if it's ever missing.
Pillow>=10.0
# Pre-staged for Oceanlab's delivery phase in BUILD_PLAN.md; currently unused
# until audio metadata extraction and YouTube delivery are implemented.
mutagen>=1.47
google-api-python-client>=2.100
google-auth-oauthlib>=1.2
