"""Shared helpers for email submodules.

Populated as helpers move out of `_legacy.py` during the split. Initial
content is empty; later steps land `_is_reserved_test_domain` + the
reserved-domain constants here.
"""
